# Keeps the GitHub Actions used by the workflows up to date. Vantage has no
# runtime dependencies; its dev tools are updated by hand.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
